{
  "name": "dns_leak_server",
  "version": "0.0.0",
  "description": "DNS Server for the DNS Leak Java library",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Mark Percival @mdp <m@mdp.im>",
  "license": "MIT",
  "dependencies": {
  }
}
